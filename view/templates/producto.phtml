<?php require './view/templates/header.phtml'; ?>

<h1>Productos</h1>

<?php foreach ($productos as $producto): ?>
    <div style="border: 1px solid #ccc; margin: 10px; padding: 10px; border-radius: 5px;">
        <h3><?= htmlspecialchars($producto->nombre) ?></h3>
        <p><?= htmlspecialchars($producto->descripcion) ?></p>
        <p><strong>$<?= $producto->precio ?></strong></p>
        <p>Categoría: <?= htmlspecialchars($producto->categoria ?? 'Sin categoría') ?></p>
        
        <a href="<?= BASE_URL ?>producto/<?= $producto->id_producto ?>">Ver más</a>
    </div>
<?php endforeach; ?>

<?php require './view/templates/footer.phtml'; ?>